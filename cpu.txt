while(まだ次の命令があるか){
  命令 = 命令列[プログラムカウンタ];
  なんかする(命令);
}

なんかする(命令){
  switch(命令){
    case なんか1:
      なんか1 の処理;
      break;
      ...
    case なんか2:
        なんか2 の処理;
        break;
  }
  プログラムカウンタを次の命令に移す;
}
